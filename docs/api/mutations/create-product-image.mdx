---
pagination_next: null
pagination_prev: null
id: create-product-image
title: createProductImage
---




export const Bullet = () => <><span style={{ fontWeight: 'normal', fontSize: '.5em', color: 'var(--ifm-color-secondary-darkest)' }}>&nbsp;●&nbsp;</span></>

export const SpecifiedBy = (props) => <>Specification<a className="link" style={{ fontSize:'1.5em', paddingLeft:'4px' }} target="_blank" href={props.url} title={'Specified by ' + props.url}>⎘</a></>

export const Badge = (props) => <><span className={props.class}>{props.text}</span></>

import { useState } from 'react';

export const Details = ({ dataOpen, dataClose, children, startOpen = false }) => {
  const [open, setOpen] = useState(startOpen);
  return (
    <details {...(open ? { open: true } : {})} className="details" style={{ border:'none', boxShadow:'none', background:'var(--ifm-background-color)' }}>
      <summary
        onClick={(e) => {
          e.preventDefault();
          setOpen((open) => !open);
        }}
        style={{ listStyle:'none' }}
      >
      {open ? dataOpen : dataClose}
      </summary>
      {open && children}
    </details>
  );
};








This mutation creates a new product image. The image is a visual representation of the product.
Image with 'TRY_ON' type will be used for try on generation.


```graphql
createProductImage(
  input: CreateProductImageInput!
): ProductImage
```




### Arguments

#### [<code style={{ fontWeight: 'normal' }}>createProductImage.<b>input</b></code>](#)<Bullet />[`CreateProductImageInput!`](/types/inputs/create-product-image-input) <Badge class="badge badge--secondary" text="non-null"/> <Badge class="badge badge--secondary" text="input"/> 
> 
> 
> 
> 

### Type

#### [`ProductImage`](/types/objects/product-image) <Badge class="badge badge--secondary" text="object"/> 
> 
> 
> ProductImage is an entity that represents an image of the product.
> Product can have multiple images. Each image can have a type.
> Only image with 'TRY_ON' type can be used for try on generation.
> There is no strict limitation on the number of images with 'TRY_ON' type.
> Please consider that only the first image with 'TRY_ON' type will be used for try on generation.
> 





